# E2E document fixtures

Documentos funcionales reutilizables por E2E se registran en
`tests/fixtures/erp-demo/canonical-demo.ts`. Los PDFs históricos que ya viven
en `mocks/` se referencian allí para evitar duplicados.
